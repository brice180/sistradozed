ngDescribe({
    name: 'Test recibidos-list component',
    modules: 'app',
    element: '<recibidos-list></recibidos-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
