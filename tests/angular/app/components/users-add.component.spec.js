ngDescribe({
    name: 'Test users-add component',
    modules: 'app',
    element: '<users-add></users-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
