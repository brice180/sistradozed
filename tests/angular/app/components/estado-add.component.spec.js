ngDescribe({
    name: 'Test estado-add component',
    modules: 'app',
    element: '<estado-add></estado-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
